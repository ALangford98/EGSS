// stb_image_write is header-only; this translation unit provides the single
// copy of its implementation for the whole engine, exactly as stb_image.cpp
// does for the reader.
#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"
